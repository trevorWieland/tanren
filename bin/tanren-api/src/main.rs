//! Tanren HTTP API — axum-based REST service.
//!
//! Exposes the tanren control plane over HTTP with:
//! - Dispatch management (create, status, cancel)
//! - Run lifecycle (provision, execute, teardown)
//! - Environment and VM management
//! - Events and metrics queries
//! - API key authentication and scoped access
//!
//! Contract-generated/validated against the shared domain model.
#![deny(clippy::disallowed_types, clippy::disallowed_methods)]

fn main() {}
