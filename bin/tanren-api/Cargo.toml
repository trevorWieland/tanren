[package]
name = "tanren-api"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Tanren HTTP API — axum-based REST service with authentication and versioning"

[lints]
workspace = true

[dependencies]

[dev-dependencies]
