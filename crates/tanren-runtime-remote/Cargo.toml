[package]
name = "tanren-runtime-remote"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Remote VM/cloud execution runtime — provider adapters and SSH transport"

[lints]
workspace = true

[dependencies]
