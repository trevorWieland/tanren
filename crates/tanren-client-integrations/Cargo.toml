[package]
name = "tanren-client-integrations"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Client Integrations subsystem: inbound webhooks, subscriptions, idempotent requests, rate limits, and backpressure for external systems calling Tanren. Excludes first-party web/API account session transport and active-account switching ownership."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
